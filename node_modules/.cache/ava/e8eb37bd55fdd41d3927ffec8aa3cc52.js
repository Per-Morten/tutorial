'use strict';

var _ava = require('ava');

var _ava2 = _interopRequireDefault(_ava);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const request = require('supertest');
const PersonModel = require('../../PersonModel');
const db = require('../database');
const mongoose = require('mongoose');

const person = {
    firstname: 'Bjarte',
    lastname: 'Larsen',
    address: 'Prost bloms gate 38',
    postcode: 2819
};

_ava2.default.cb.before(t => {
    db('person-api-test').then(() => t.end()).catch(err => t.fail(err));
});
_ava2.default.cb.after(t => {
    mongoose.connection.db.dropDatabase(() => {
        mongoose.connection.close();
        t.end();
    });
});

_ava2.default.cb.beforeEach(t => {
    PersonModel.remove({}, () => {
        PersonModel.createUser(Object.assign({}, person)).then(user => {
            if (user) t.end();
        }).catch(err => t.fail(`Failed beforeeach with error ${err}`));
    });
});

const server = require('../../../classes/App.js');
//# sourceMappingURL=../../../node_modules/.cache/ava/e8eb37bd55fdd41d3927ffec8aa3cc52.js.map